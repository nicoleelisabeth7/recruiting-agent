{
  "name": "recruiting-agent-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker for role-agnostic candidate scoring via Slack",
  "main": "src/worker.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "itty-router": "^4.0.0",
    "pdfjs-dist": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231121.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.26.0"
  }
}
